# Mobile

React Native app. From the monorepo root you can run:

```bash
yarn workspace mobile start
```

This package expects Expo or React Native CLI setup. Add dependencies in `package.json` as needed.
