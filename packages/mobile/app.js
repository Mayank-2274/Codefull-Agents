// Standard Expo entry point - imports the App component from src
import App from './src/App';

// Export the App component as the default export
export default App;

// No styles needed as they're in the imported App component